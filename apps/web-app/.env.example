# Database (Required)
# Use a PostgreSQL database provider like Neon, Supabase, or Vercel Postgres
DATABASE_URL="postgresql://user:password@host:5432/database?sslmode=require"

# Authentication (Required)
# Generate with: openssl rand -base64 32
JWT_SECRET="your-super-secret-jwt-key-minimum-32-characters-long"
JWT_REFRESH_SECRET="your-super-secret-refresh-key-minimum-32-characters-long"

# API URL (Optional)
# Leave empty for Vercel deployment (uses relative paths)
# Set to external API URL if using separate backend
NEXT_PUBLIC_API_URL=""

# AI Engine URL (Optional)
# Leave empty for integrated Python functions
NEXT_PUBLIC_AI_URL=""
